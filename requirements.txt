fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.5
python-dotenv>=1.0
scikit-learn>=1.4
numpy>=1.26

# AI providers
anthropic>=0.40
google-generativeai>=0.8
openai>=1.50          # used for xAI Grok and OpenAI-compatible endpoints

# Optional: Gemma local inference via Ollama (no pip install needed — runs as sidecar)
